apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: le-wildcard-issuer
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: chanju0804@gmail.com
    privateKeySecretRef:
      name: le-wildcard-issuer
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-api-token
              key: api-token